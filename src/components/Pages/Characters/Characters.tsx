import React, { ReactElement } from 'react'

interface Props {
    
}

export default function Characters({}: Props): ReactElement {
    return (
        <div>
            <h1>Characters</h1>
        </div>
    )
}
